[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "vacuum-decay-2-fields"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["base*", "G_Y_WKB*"]
